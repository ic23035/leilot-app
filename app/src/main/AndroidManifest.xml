<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.leilotparkingstart">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeiLotParkingStart"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".list_activity"
            android:exported="true"
            android:label="@string/title_activity_parking_list" />

        <activity
            android:name=".ParkingActivity"
            android:exported="true"
            android:label="@string/PA_name"/>

        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FormActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LeiLotParkingStart.NoActionBar">
        </activity>

        <activity
            android:name=".CardPayment"
            android:exported="true" />
        <activity
            android:name=".WalletMain"
            android:exported="true">
        </activity>

        <activity
            android:name=".StatsActivity"
            android:exported="true">
        </activity>


    </application>

</manifest>